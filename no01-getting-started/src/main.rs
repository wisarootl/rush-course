/*

- init project
```
cargo init <project-name>
```

- related rust command
```
rustc
rustup
rustfmt
```

- cargo sub command
```
cargo build (--release)
cargo run (--quiet)
cargo check
cargo fmt
```

*/

fn main() {
    println!("Hello, world!");
}
